import { CentrarPieDirective } from './centrar-pie.directive';

describe('CentrarPieDirective', () => {
  it('should create an instance', () => {
    const directive = new CentrarPieDirective();
    expect(directive).toBeTruthy();
  });
});
